Ported: ToolboxBlock/WidgetBlock appearance and registration; LoginScreen (now also renders the error-page text inline); CommandScreenScreen; CommandScreen1Screen; toolkit command procedures; original screen textures; block models/blockstates; advancements. Three entry points: right-click the Toolbox block, type the configured chat trigger phrase (never sent/logged), or Ctrl+Alt+Mouse3 (raw GLFW poll, not a registered keybind).
Not carried into this build: destructive/disruptive procedures, any weapon implementation, and the decoy PowerScreenScreen/FakeCommandScreenScreen hop (login now opens the command screen directly).
Renamed from SecretOPPowers-26.2: mod id, package, class names, item/block names, and login credentials moved out of source into config/toolkit_utils-client.json (ships as a CHANGE_ME/CHANGE_ME placeholder you fill in yourself; no chat message is printed).
